{
  "name": "aswan-food-delivery",
  "version": "1.0.0",
  "description": "Aswan Food Delivery Platform - Full Stack Application",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci --legacy-peer-deps && npm run build",
    "start": "cd client && npm start",
    "install-client": "cd client && npm ci --legacy-peer-deps",
    "build-client": "cd client && npm run build",
    "install-all": "npm ci --legacy-peer-deps && cd client && npm ci --legacy-peer-deps"
  },
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {
    "npm": "^8.0.0"
  }
}